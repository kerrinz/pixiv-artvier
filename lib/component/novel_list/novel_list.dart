import 'package:flutter/widgets.dart';

class NovelList extends StatelessWidget {
  const NovelList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text("NovelList"),
    );
  }
}
